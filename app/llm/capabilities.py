"""Единственный источник правды о функционале бота — для инструмента bot_help.

Plain-markdown, уходит модели как результат инструмента. При добавлении
команд/фич обновлять ЗДЕСЬ (тест test_capabilities следит, чтобы все команды
из меню были упомянуты).
"""

CAPABILITIES = """Полное описание возможностей Умного Геннадия.

**Общение**
- Личка: отвечаю на всё; серия сообщений подряд получает один общий ответ.
- Группы: отвечаю на @упоминание или reply на моё сообщение; вижу, на какое
  сообщение реплаят. С шансом /proactive (0-100%) сам вставляю реплику.
- Хартбит: иногда сам пишу первым (напомнить о деле, вернуться к оборванной
  теме, спросить как прошло) — по таймеру, только при реальном поводе и не по
  ночам. Включено по умолчанию. Выключить: /heartbeat off (обратно /heartbeat
  on; /heartbeat 240 — реже/чаще, минуты). Если моя самоинициация мешает —
  подскажи собеседнику /heartbeat off.
- /persona описание — мой характер в этом чате. /model — какая LLM за меня
  думает (список моделей: /model list; сменить может админ: /model vendor/model,
  вернуть: /model reset).
- /undo — стереть последний обмен из моей истории. /retry — перегенерировать
  последний ответ (/retry smart — умной моделью). /search запрос — поиск по
  памяти и переписке чата.

**Мультимодальность**
- Понимаю голосовые (транскрибирую и отвечаю), фото (в личке сразу, в группе
  по обращению; reply на фото + вопрос тоже работает; про недавнее фото можно
  спросить и без reply — «что на фото?» — сам подтяну его из истории),
  документы PDF/DOCX/TXT до 20 МБ. Могу сгенерировать картинку — просто попроси.

**Долгая память**
- Сам запоминаю важные устойчивые факты; попроси «запомни: …» для явного.
- /memory — что помню; /forget номер — забыть. Похожий новый факт заменяет
  старый. Релевантные факты сам подтягиваю в контекст.

**Напоминания и задачи**
- Словами: «напомни завтра в 9…», «сделай в понедельник…» (агент-задача —
  выполню с инструментами), поддерживается cron-повторение.
- /tasks — список; /tasks cancel номер — отменить.

**Интернет и код**
- Ищу в вебе (web_search) и читаю страницы (fetch_url) сам, когда нужно.
- Выполняю Python в изолированной песочнице (есть интернет, 30 сек, без
  доступа к моим данным). Удачный код можно сохранить: «сохрани скрипт…»,
  /scripts — список, запуск: «запусти скрипт имя».

**Скиллы** (именованные сценарии чата)
- Создать: «создай скилл имя, который …» или /skill add имя инструкция.
- Запуск: /имя-скилла [аргумент], вебхуком, по расписанию (/skill cron имя
  выражение) или мной самим как шаг работы.
- Безопасность: /skill tools имя список,масок — ограничить инструменты
  (например mcp_hubhead_*); /skill show|list|edit|on|off|remove.

**MCP-серверы** (подключение внешних сервисов: Todoist, Notion, свои)
- /mcp add имя url [токен] — инструменты сервера сразу доступны мне в чате.
- Если сервер требует OAuth — пришлю ссылку «Разрешить» (/mcp auth имя для
  переавторизации). /mcp list|on|off|refresh|remove.

**Входящие вебхуки** (события из внешних сервисов в этот чат)
- /hook add имя — уведомления о POST-событиях на выданный URL.
- /hook add имя инструкция — на каждое событие выполняю инструкцию.
- /hook add имя skill:имя-скилла — событие обрабатывает скилл (с его
  ограничениями инструментов). /hook list|on|off|delete.

**Деньги и статистика**
- /stats — расходы чата и топ участников; /budget — месячный лимит;
  /digest ЧЧ:ММ — ежедневный отчёт расходов в личку (выкл: /digest off).

**Прочее**
- /start — знакомство и быстрая настройка характера.
- /tools — включение/выключение моих инструментов в чате (админ).
- /whoami — кто ты для меня и что это за workspace.
- Админ: /invite и /kick (id или reply) — управление whitelist'ом,
  /users — список, /dashboard — веб-дашборд (только localhost).

Всё (память, скиллы, MCP, вебхуки, скрипты, бюджет) живёт отдельно в каждом
чате-workspace'е. Настраивать скиллы/MCP/вебхуки может любой участник чата.
"""
